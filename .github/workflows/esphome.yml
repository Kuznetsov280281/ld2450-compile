name: ESPHome Compile

on:
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      
    - name: Compile with Docker
      run: |
        docker run --rm -v $(pwd):/config esphome/esphome compile lidar.yaml
        
    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: ld2450-firmware
        path: |
          .esphome/build/ld2450-radar/.pioenvs/ld2450-radar/firmware.bin
          .esphome/build/ld2450-radar/.pioenvs/ld2450-radar/firmware.factory.bin
          .esphome/build/ld2450-radar/.pioenvs/ld2450-radar/firmware.ota.bin
